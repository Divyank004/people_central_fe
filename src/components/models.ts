export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Employee {
  id: number;
  name: string;
  vacations: number;
  year: number;
  role: string
}